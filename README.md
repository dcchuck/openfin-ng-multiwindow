# OpenfinNgMultiwindow

OpenFin POC for Angular Multi-Window integration

--

1. Run `npm install`

2. Run `ng serve`

3. `http://localhost:4200/`

4. ```bash
npm i -g openfin-cli
openfin -l -c app.json
```
